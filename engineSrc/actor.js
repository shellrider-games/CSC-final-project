/*
* abstract class for an Actor, an Actor is an object you can add to the engine and is updated and rendered every frame
*/

class Actor {
  constructor() {}

  update(delta) {}
  render() {}
}

export default Actor;
